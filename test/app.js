require('./helper');
var describe = require('mocha').describe,
    it = require('mocha').it,
    expect = require('chai').expect,
    beforeEach = require('mocha').beforeEach,
    $ = require('jquery');

describe('Testing App', function () {
  it('should start with 1', function () {
    // expect($('.form').length).equal(0);
  });

});
